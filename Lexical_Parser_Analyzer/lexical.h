#ifndef LEXICAL_H
#define LEXICAL_H

#include <vector>
#include <cctype>
using namespace std;
// Function to process a vector of strings and return a processed result (for example, tokenize, lex analysis, etc.)
void lexical(const vector<char>& vec);

#endif // LEXICAL_H
